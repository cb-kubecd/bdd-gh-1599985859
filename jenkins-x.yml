buildPack: go
pipelineConfig:
  pipelines:
    overrides:
      - name: container-build
        step: 
          sh: jx promote -b --all-auto --timeout 1h
          name: jx-promote
          image: gcr.io/jenkinsxio/jx-promote:0.0.94
